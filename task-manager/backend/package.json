{
  "name": "kanban-task-manager-api",
  "version": "1.0.0",
  "private": true,
  "description": "Express and PostgreSQL API for a Kanban task manager",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "db:migrate": "node src/scripts/migrate.js",
    "test": "jest --runInBand --passWithNoTests",
    "lint": "eslint src"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "eslint": "^10.10.0",
    "jest": "^30.5.1",
    "nodemon": "^3.1.14"
  }
}
